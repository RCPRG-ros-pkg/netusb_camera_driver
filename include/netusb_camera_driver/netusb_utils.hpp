#ifndef NETUSB_UTILS
#define NETUSB_UTILS

#include <string>

std::string err2str(int errcode);

std::string param2str(int param);

char chariftrue(bool param, char ch);

#endif // NETUSB_UTILS
